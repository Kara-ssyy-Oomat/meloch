# 📱 Исправление ошибок Android приложения "Кербен"

## ✅ Что было исправлено:

### 1. Имя приложения изменено на "Кербен"
- **manifest.json** - оба файла обновлены
- **capacitor.config.json** - название и appId изменены
- **index.html** - мета-теги и title обновлены

### 2. Исправлена ошибка открытия
**Проблема:** start_url был "/" вместо "/index.html"

**Решение:**
```json
"start_url": "/index.html"
```

### 3. Добавлено автообновление
- Создан **sw.js** - Service Worker для PWA
- Автоматическая проверка обновлений каждые 5 минут
- Уведомление пользователя о новой версии
- Автоматическая перезагрузка после обновления

---

## 🔧 Что делать дальше:

### Шаг 1: Пересоздать APK файл

```bash
cd прило
npx cap sync android
npx cap open android
```

Затем в Android Studio:
1. Build → Clean Project
2. Build → Rebuild Project  
3. Build → Build Bundle(s) / APK(s) → Build APK(s)

### Шаг 2: Проверить иконки приложения

Убедитесь что файлы существуют:
- `/icon-192.png` (192x192 пикселей)
- `/icon-512.png` (512x512 пикселей)

Если их нет - создайте иконку с логотипом "Кербен"

### Шаг 3: Загрузить на GitHub и хостинг

```bash
git add .
git commit -m "Исправление: имя Кербен, автообновление, исправление ошибок"
git push
```

---

## 🚀 Как работает автообновление:

### Когда вы добавляете новую функцию на сайт:

1. ✅ **Загружаете изменения на GitHub**
2. ✅ **Приложение автоматически проверяет обновления каждые 5 минут**
3. ✅ **Показывается уведомление "Доступна новая версия"**
4. ✅ **Пользователь нажимает "Обновить" и получает новую версию**

### ⚠️ Важно:

- **НЕ НУЖНО** пересобирать APK при каждом обновлении сайта!
- APK пересобирается ТОЛЬКО если меняете:
  - Название приложения
  - Иконку
  - Разрешения (permissions)
  - Версию приложения

- **Обновления контента автоматические:**
  - Новые товары
  - Изменения дизайна
  - Новые функции
  - Исправления багов

---

## 🔍 Проверка работы:

### 1. Проверить Service Worker:
1. Откройте сайт в Chrome
2. F12 → Application → Service Workers
3. Должен быть статус "activated and is running"

### 2. Проверить манифест:
1. F12 → Application → Manifest
2. Проверить:
   - Name: "Кербен"
   - Start URL: "/index.html"
   - Icons загружены

### 3. Тест установки PWA:
1. Откройте сайт в Chrome на телефоне
2. Меню → "Добавить на главный экран"
3. Проверить имя: должно быть "Кербен"
4. Запустить - должно открыться без ошибок

---

## ❗ Если приложение не открывается:

### Проверьте в capacitor.config.json:

```json
{
  "server": {
    "url": "https://ваш-домен.github.io",
    "cleartext": true
  }
}
```

Или для локальной разработки:
```json
{
  "server": {
    "androidScheme": "https"
  }
}
```

---

## 📝 Обновленные файлы:

1. ✅ manifest.json (корень)
2. ✅ прило/manifest.json  
3. ✅ прило/capacitor.config.json
4. ✅ index.html
5. ✅ sw.js (новый файл)

---

## 💡 Совет:

При первой установке APK некоторые телефоны могут показывать предупреждение "Неизвестный источник" - это нормально для APK не из Google Play. Разрешите установку из неизвестных источников в настройках безопасности.
